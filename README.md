# Asteroids

A classic Asteroids game implementation using Python and Pygame, for Boot.dev course


## Installation

Install dependencies using uv:

```bash
uv sync
```

## Running

```bash
python main.py
```

## Controls

- `W` / `S` - Move forward/backward
- `A` / `D` - Rotate left/right
- `SPACE` - Shoot
